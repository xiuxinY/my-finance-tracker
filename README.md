# my-finance-tracker

一个基于 **React + Django + FastAPI + MySQL** 的全栈 Web 记账与预算管理应用。
目标是提供一个跨设备使用的个人财务管理工具，包括：

* 消费记录（CRUD）
* 多账户管理
* 分类管理
* 预算设置 / 预算进度展示
* 图表与统计分析
* 管理后台（Django Admin）
* 后续支持：AI 消费预测、消费分类自动识别

本项目采用分层架构，前后端完全分离，并通过统一 API 通信。

---

# 🏗️ 系统架构概要

```
           ┌───────────────────────┐
           │        React          │
           │   (前端界面 + 图表)     │
           └─────────▲─────────────┘
                     │  HTTP/JSON
                     ▼
     ┌──────────────────────────────────────┐
     │         API Gateway / Nginx          │
     │   /api/core/* → Django               │
     │   /api/analytics/* → FastAPI         │
     └───────────▲──────────────────────────┘
                 │
   ┌─────────────┴─────────────┐
   │          Django             │
   │ CRUD / 权限 / Admin / ORM   │
   └─────────────▲─────────────┘
                 │   共用 MySQL
                 ▼
          ┌──────────────┐
          │    MySQL      │
          │ 数据存储 / ORM │
          └──────────────┘

           FastAPI
   ┌──────────────────────────┐
   │ 报表统计 / 聚合查询 / AI │
   │ 月度趋势 / 分类占比      │
   └──────────────────────────┘
```

---

# 🧰 技术选型

## 前端（React）

* React 18
* React Router
* Axios（调用 API）
* ECharts（图表）
* Tailwind CSS / MUI（二选一）

## 后端 1（Django）

* Django 5.x
* Django REST Framework (DRF)
* Django Admin 用于后台管理
* JWT 认证（SimpleJWT）

## 后端 2（FastAPI）

* FastAPI + Uvicorn
* Pydantic
* SQLAlchemy（读写 MySQL / 聚合查询）
* 将来可扩展：AI 推理 / 异步任务

## 数据库

* MySQL 8.x（Docker 容器方式）

## DevOps（可选）

* Docker Compose 管理全栈服务
* Nginx 作为反向代理
* Pre-commit / Black / Ruff（代码风格）

---

# 📂 项目目录结构（建议）

```
personal-finance-app/
│
├── frontend/                 # React 前端
│   ├── src/
│   ├── public/
│   └── package.json
│
├── backend-django/           # Django（CRUD + Admin）
│   ├── finance_core/
│   │   ├── models.py         # 所有数据库模型
│   │   ├── serializers.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── admin.py
│   ├── finance_core_api/
│   ├── settings.py
│   └── manage.py
│
├── backend-fastapi/          # FastAPI（统计/报表）
│   ├── app/
│   │   ├── main.py
│   │   ├── routers/
│   │   │   └── analytics.py
│   │   ├── models/
│   │   └── deps.py
│   └── requirements.txt
│
├── nginx/
│   └── nginx.conf            # 反向代理到两个后端
│
├── docker-compose.yml         # 一键启动整个系统
└── README.md                  # 本文档
```

---

# 📊 功能模块设计

## 1. 消费记录（Transaction）

* 创建 / 查询 / 修改 / 删除（CRUD）
* 字段：amount, category, account, time, remark, tags
* 后端：Django + DRF
* 图表：FastAPI 提供聚合，React 绘图

## 2. 分类管理（Category）

* 分类 CRUD
* 分类颜色、排序
* Django Admin 管理

## 3. 多账户（Account）

* 微信、现金、银行卡、信用卡
* 后续可做资产统计

## 4. 预算系统（Budget）

* 月度预算总额
* 分类预算
* 预算进度条 / 超支提醒

## 5. 报表系统（FastAPI）

提供接口：

* 月度趋势：`GET /api/analytics/monthly-trend`
* 分类占比：`GET /api/analytics/category-breakdown`
* 预算状态：`GET /api/analytics/budget-status`

所有图表都由 React + ECharts 实现。

---

# 🔐 用户与安全设计

* 统一使用 Django 完成用户认证
* 登录时返回 JWT
* React 前端保存 token（localStorage）
* 所有请求都携带 Authorization Bearer
* FastAPI 与 Django 共享 JWT 验证秘钥

---

# 🔌 API 设计（示例）

## 核心 CRUD（Django）

```
GET    /api/core/transactions/
POST   /api/core/transactions/
GET    /api/core/transactions/{id}
PUT    /api/core/transactions/{id}
DELETE /api/core/transactions/{id}
```

## 报表 API（FastAPI）

```
GET /api/analytics/category-breakdown?month=2025-12
GET /api/analytics/monthly-trend?year=2025
GET /api/analytics/budget-status
```

---

# 🗂️ 开发阶段计划（核心）

## **阶段 1：最小可行产品（MVP）**

* FastAPI + React + MySQL
* 输入交易、查看列表
* 分类管理
* 月度汇总
* ECharts 饼图、折线图

## **阶段 2：加入 Django（管理能力 + CRUD）**

* 添加 Django + DRF
* 加入 Django Admin
* 用户体系迁移到 Django
* 所有 CRUD 统一到 `/api/core`

## **阶段 3：系统集成与优化**

* 加 Nginx 反向代理
* 完整 Docker Compose
* 日志、错误监控
* 部署准备

## **阶段 4：高级功能（可选）**

* 多账户资产管理
* 消费预测（FastAPI + 简单 ML 模型）
* AI 分类推荐
* 导入账单（CSV/Excel）
* 手机端适配

---

# 🚀 如何运行（开发模式）

## 1. 启动 MySQL（Docker）

```
docker-compose up -d mysql
```

## 2. 启动 Django

```
cd backend-django
python manage.py migrate
python manage.py runserver 8000
```

## 3. 启动 FastAPI

```
cd backend-fastapi
uvicorn app.main:app --reload --port 8001
```

## 4. 启动 React 前端

```
cd frontend
npm install
npm start
```

---

# 📝 许可证

MIT License

---

# 🧑‍💻 作者

Cody —— Digital Project Manager / Data Scientist / AI Engineer
本项目用于个人学习全栈架构、数据分析、API 设计与工程化实践。
